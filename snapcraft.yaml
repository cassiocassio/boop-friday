name: boop-friday
version: 0.1
summary: start somewhere
description: Less than a hundred word summary is likely. But not certain.
grade: devel # must be 'stable' to release into candidate/stable channels
base: core18
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  boop-friday:
    plugin: python
    python-version: python3
    source: .
apps:
  boop-friday:
  command: bin/boop-friday.py
